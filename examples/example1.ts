// import { createClient } from "kickbot-sdk";
// import { ClientOptions } from "kickbot-sdk/types/options";

// const options: ClientOptions = {
//   channelName: "myChannel",
//   logging: true,
// };

// const client = createClient(options);

// client.connect();

// client.onMessage((message) => {
//   console.log(`New message: ${message}`);
// });

// client.onSubscription((subscriber) => {
//   console.log(`New subscriber: ${subscriber}`);
// });

// // Moderation
// client.moderate("ban", "userId123");
